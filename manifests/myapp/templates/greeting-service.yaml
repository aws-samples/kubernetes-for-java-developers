apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-greeting
  labels:
    {{- template "labels" . }}
spec:
  selector:
    app: {{ .Release.Name }}-greeting
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: http
  type: LoadBalancer